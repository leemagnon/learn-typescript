// JS 문자열 선언
// var str = "hello";

// TS 문자열 선언
let str: string = "hello";

// 숫자 
const num: number = 10;

// 배열
let arr: Array<number> = [1, 2, 3];
let items: number[] = [1, 2 ,3];

let heroes: Array<string> = ["Capt", "Thor", "Hulk"];

// 튜플: 요소의 타입까지 지정
let address: [string, number] = ['gangnam', 127];

// 객체
let obj: object = {};
// let person: object = {
//     name: 'Mark',
//     age: 22
// };
let person: {name: string, age: number} = {
    name: 'Mark',
    age: 22
};

// 진위값
let show: boolean = true;
